name: Sync volume-i to monorepo

on:
  push:
    branches: [main]
    paths:
      - 'volume-i/**'

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout lra-volume-i
        uses: actions/checkout@v4
        with:
          path: source

      - name: Checkout monorepo
        uses: actions/checkout@v4
        with:
          repository: wsollers/Learning-Real-Analysis
          token: ${{ secrets.SYNC_PAT }}
          path: monorepo

      - name: Validate leaf proof stubs
        run: |
          cd source
          python scripts/validate_leaf_proofs.py --root . --strict

      - name: Sync volume-i/ into monorepo
        run: |
          rsync -av --delete source/volume-i/ monorepo/volume-i/

      - name: Commit and push if changed
        run: |
          cd monorepo
          git config user.name  "lra-sync-bot"
          git config user.email "sync-bot@noreply.github.com"
          git add volume-i/
          git diff --cached --quiet && echo "No changes to sync" && exit 0
          git commit -m "chore: sync volume-i from lra-volume-i"
          git push
