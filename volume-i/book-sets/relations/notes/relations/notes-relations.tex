% =========================================================
% Ordered Pairs and Relations
% =========================================================

\subsection{Ordered Pairs and Relations}

% ---------------------------------------------------------
% TOOLKIT
% ---------------------------------------------------------

\begin{remark*}[Geometric picture]
A binary relation from $A$ to $B$ can be pictured as directed edges from the
$A$-side to the $B$-side of a bipartite diagram. The converse reverses every
edge, and composition follows two edges in series through an intermediate set.
\end{remark*}

\begin{definitionbox}{Definition (Ordered Pair)}
\begin{definition}[Ordered Pair]\label{def:ordered-pair}
Let $a$ and $b$ be sets. The \emph{ordered pair} $(a,b)$ is defined (Kuratowski)
as
\[
(a,b) \;:=\; \{\{a\}, \{a,b\}\}.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall a\,\forall b\,\exists p\,
  \bigl(p=(a,b)\wedge p=\{\{a\},\{a,b\}\}\bigr).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{OrderedPair}(p,a,b)
  \iff
  p=\{\{a\},\{a,b\}\}.
\]
\end{remark*}

\begin{remark*}[Interpretation]
An ordered pair packages two objects in a way that remembers which component
is first and which component is second.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[ax:pairing]{Axiom of Pairing}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem (Uniqueness of Ordered Pairs)}
\begin{theorem}[Uniqueness of Ordered Pairs]\label{thm:ordered-pair-unique}
For any sets $a,b,c,d$,
\[
(a,b) = (c,d)
\;\;\Longleftrightarrow\;\;
(a = c \land b = d).
\]
\hyperref[prf:ordered-pair-unique]{Go to proof.}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
$\forall a, b, c, d:\; (a,b) = (c,d) \;\leftrightarrow\; (a = c \wedge b = d)$.
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{OrderedPair}(p,a,b)
  \wedge
  \operatorname{OrderedPair}(q,c,d)
  \wedge p=q
  \iff
  (a=c\wedge b=d).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Equality of ordered pairs is componentwise equality. The theorem is the
justification for using ordered-pair notation as ordered data rather than as
an unordered set.
\end{remark*}

\begin{remark*}[Exposition]
The purpose of the Kuratowski definition is purely to guarantee the uniqueness
theorem above. Once $(a,b) = (c,d) \iff a=c \land b=d$ is established,
ordered pairs may be treated as primitives with this property and the
encoding.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:ordered-pair]{Ordered Pair}
  \item \hyperref[ax:pairing]{Axiom of Pairing}
  \item \hyperref[ax:extensionality]{Axiom of Extensionality}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Cartesian Product --- as foundation for relations)}
\begin{definition}[Cartesian Product --- as foundation for relations]\label{def:cartesian-product-rel}
The \emph{Cartesian product} of sets $A$ and $B$ is:
\[
A \times B
\;:=\;
\{\, (a,b) \mid a \in A \text{ and } b \in B \,\}.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall A\,\forall B\,\forall x,\quad
  x\in A\times B
  \iff
  \exists a\in A\,\exists b\in B,\ x=(a,b).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{CartesianProduct}(P,A,B)
  \iff
  \forall x,\ x\in P \leftrightarrow
  \exists a\in A\,\exists b\in B,\ x=(a,b).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The Cartesian product is the set of all ordered pairs whose first coordinate
comes from $A$ and whose second coordinate comes from $B$.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:ordered-pair]{Ordered Pair}
  \item \hyperref[ax:pairing]{Axiom of Pairing}
  \item \hyperref[ax:power-set]{Axiom of Power Set}
  \item \hyperref[ax:separation]{Axiom Schema of Separation}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Relation)}
\begin{definition}[Relation]\label{def:relation}
A \emph{relation} from $A$ to $B$ is any subset $R \subseteq A \times B$.

If $(a,b) \in R$ we write $a \, R \, b$ and say ``$a$ is related to $b$.''
When $A = B$ we say $R$ is a \emph{relation on $A$}.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  R \text{ is a relation from } A \text{ to } B
  \iff
  R\subseteq A\times B.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationFrom}(R,A,B)
  \iff
  R\subseteq A\times B.
\]
\[
  \operatorname{RelationOn}(R,A)
  \iff
  \operatorname{RelationFrom}(R,A,A).
\]
\end{remark*}

\begin{remark*}[Interpretation]
A relation is a set of ordered pairs. It may relate some inputs to no outputs,
one output, or many outputs unless additional function-like conditions are
imposed later.
\end{remark*}

\begin{remark*}[Exposition]
Relations introduce no new foundational objects: they are simply sets of
ordered pairs, constructed using operations already available. Properties of
relations can therefore be studied with ordinary set-theoretic reasoning.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:cartesian-product-rel]{Cartesian Product}
  \item \hyperref[def:subset]{Subset}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Domain of a Relation)}
\begin{definition}[Domain of a Relation]\label{def:relation-domain}
Let $R\subseteq A\times B$ be a relation. The \emph{domain} of $R$ is
\[
\operatorname{Dom}(R)
\;:=\;
\{\,a\in A\mid \exists b\in B,\ (a,b)\in R\,\}.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall a\in A,\quad
  a\in\operatorname{Dom}(R)
  \iff
  \exists b\in B,\ (a,b)\in R.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationDomainElement}(a,R,A,B)
  \iff
  a\in A \wedge \exists b\in B,\ (a,b)\in R.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The domain of a relation records exactly the first coordinates that actually
occur in related pairs.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[ax:separation]{Axiom Schema of Separation}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Range of a Relation)}
\begin{definition}[Range of a Relation]\label{def:relation-range}
Let $R\subseteq A\times B$ be a relation. The \emph{range} of $R$ is
\[
\operatorname{Ran}(R)
\;:=\;
\{\,b\in B\mid \exists a\in A,\ (a,b)\in R\,\}.
\]
Tarski also calls this class the \emph{counter-domain} or \emph{converse domain}.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall b\in B,\quad
  b\in\operatorname{Ran}(R)
  \iff
  \exists a\in A,\ (a,b)\in R.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationRangeElement}(b,R,A,B)
  \iff
  b\in B \wedge \exists a\in A,\ (a,b)\in R.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The range of a relation records exactly the second coordinates that actually
occur in related pairs.
\end{remark*}

\begin{remark*}[Exposition]
The domain records the first coordinates that occur in a relation; the range
records the second coordinates that occur.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[ax:separation]{Axiom Schema of Separation}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Converse Relation)}
\begin{definition}[Converse Relation]\label{def:converse-relation}
If $R\subseteq A\times B$, the \emph{converse} of $R$ is the relation
$R^{-1}\subseteq B\times A$ defined by
\[
(b,a)\in R^{-1}
\;\Longleftrightarrow\;
(a,b)\in R.
\]
Equivalently, $b\,R^{-1}\,a$ iff $aRb$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall a\in A\,\forall b\in B,\quad
  (b,a)\in R^{-1}
  \iff
  (a,b)\in R.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{ConverseRelationElement}(b,a,R)
  \iff
  (a,b)\in R.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The converse relation reverses every ordered pair of the original relation.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:ordered-pair]{Ordered Pair}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Identity Relation)}
\begin{definition}[Identity Relation]\label{def:relation-identity}
For a set $A$, the \emph{identity relation} on $A$ is
\[
\Delta_A
\;:=\;
\{\, (a,a)\mid a\in A\,\}.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall x\,\forall y,\quad
  (x,y)\in\Delta_A
  \iff
  x\in A \wedge y\in A \wedge x=y.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{IdentityRelation}(\Delta_A,A)
  \iff
  \forall x\,\forall y,\ ((x,y)\in\Delta_A \leftrightarrow x\in A \wedge y\in A \wedge x=y).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The identity relation is the diagonal relation on $A$: each element is related
only to itself.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:cartesian-product-rel]{Cartesian Product}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Composition of Relations)}
\begin{definition}[Composition of Relations]\label{def:relation-composition}
Let $R\subseteq A\times B$ and $S\subseteq B\times C$. The
\emph{composition} $S\circ R\subseteq A\times C$ is defined by
\[
(a,c)\in S\circ R
\;\Longleftrightarrow\;
\exists b\in B,\; (a,b)\in R \text{ and } (b,c)\in S.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall a\in A\,\forall c\in C,\quad
  (a,c)\in S\circ R
  \iff
  \exists b\in B,\ ((a,b)\in R\wedge(b,c)\in S).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationCompositionElement}(a,c,S,R)
  \iff
  \exists b,\ ((a,b)\in R\wedge(b,c)\in S).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Relation composition follows an $R$-step and then an $S$-step through a shared
middle element.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:cartesian-product-rel]{Cartesian Product}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem (Associativity of Relation Composition)}
\begin{theorem}[Associativity of Relation Composition]\label{thm:relation-composition-assoc}
Let $R\subseteq A\times B$, $S\subseteq B\times C$, and
$T\subseteq C\times D$. Then
\[
T\circ(S\circ R)=(T\circ S)\circ R.
\]
\hyperref[prf:relation-composition-assoc]{Go to proof.}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
For all $a\in A$ and $d\in D$, $(a,d)\in T\circ(S\circ R)$ iff there are
$b\in B$ and $c\in C$ with $(a,b)\in R$, $(b,c)\in S$, and $(c,d)\in T$,
which is exactly the membership condition for $(a,d)\in (T\circ S)\circ R$.
\end{remark*}

\begin{remark*}[Interpretation]
Associativity says that a three-step relational path has the same endpoints
regardless of whether the first two steps or the last two steps are grouped
first.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation-composition]{Composition of Relations}
  \item \hyperref[def:relation-equality]{Relation Equality}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem (Identity Laws for Relation Composition)}
\begin{theorem}[Identity Laws for Relation Composition]\label{thm:relation-composition-id}
Let $R\subseteq A\times B$. Then
\[
R\circ\Delta_A=R
\quad\text{and}\quad
\Delta_B\circ R=R.
\]
\hyperref[prf:relation-composition-id]{Go to proof.}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
  \forall R\subseteq A\times B,\quad
  R\circ\Delta_A=R
  \wedge
  \Delta_B\circ R=R.
\]
\end{remark*}

\begin{remark*}[Interpretation]
Composing with the identity relation leaves a relation unchanged on the
appropriate side, just as composing a function with an identity function leaves
the function unchanged.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation-identity]{Identity Relation}
  \item \hyperref[def:relation-composition]{Composition of Relations}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Null Relation)}
\begin{definition}[Null Relation]\label{def:null-relation}
For sets $A$ and $B$, the \emph{null relation} from $A$ to $B$ is
$\varnothing\subseteq A\times B$. No element of $A$ is related to any element
of $B$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  R=\varnothing \subseteq A\times B
  \iff
  \forall a\in A\,\forall b\in B,\ (a,b)\notin R.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{NullRelation}(R,A,B)
  \iff
  R=\varnothing \wedge R\subseteq A\times B.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The null relation has no arrows from $A$ to $B$.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:empty-set]{Empty Set}
  \item \hyperref[def:cartesian-product-rel]{Cartesian Product}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Universal Relation)}
\begin{definition}[Universal Relation]\label{def:universal-relation}
For sets $A$ and $B$, the \emph{universal relation} from $A$ to $B$ is
$A\times B$ itself. Every element of $A$ is related to every element of $B$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  R=A\times B
  \iff
  \forall a\in A\,\forall b\in B,\ (a,b)\in R.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{UniversalRelation}(R,A,B)
  \iff
  R=A\times B.
\]
\end{remark*}

\begin{remark*}[Interpretation]
The universal relation relates every element of $A$ to every element of $B$.
\end{remark*}

\begin{remark*}[Exposition]
The null relation and universal relation are the two extreme binary relations
from $A$ to $B$.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:cartesian-product-rel]{Cartesian Product}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Relation Inclusion)}
\begin{definition}[Relation Inclusion]\label{def:relation-inclusion}
Let $R,S\subseteq A\times B$. Relation inclusion is ordinary set inclusion:
\[
R\subseteq S
\;\Longleftrightarrow\;
\forall a\in A\,\forall b\in B,\ ((a,b)\in R\Rightarrow (a,b)\in S),
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  R\subseteq S
  \iff
  \forall a\in A\,\forall b\in B,\ ((a,b)\in R\to(a,b)\in S).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationInclusion}(R,S,A,B)
  \iff
  \forall a\in A\,\forall b\in B,\ ((a,b)\in R\to(a,b)\in S).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Relation inclusion is ordinary subset inclusion applied to sets of ordered
pairs.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:subset]{Subset}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Relation Equality)}
\begin{definition}[Relation Equality]\label{def:relation-equality}
Let $R,S\subseteq A\times B$. Relation equality is ordinary set equality:
\[
R=S
\;\Longleftrightarrow\;
R\subseteq S \text{ and } S\subseteq R.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  R=S
  \iff
  \operatorname{RelationInclusion}(R,S,A,B)
  \wedge
  \operatorname{RelationInclusion}(S,R,A,B).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationEquality}(R,S,A,B)
  \iff
  \operatorname{RelationInclusion}(R,S,A,B)
  \wedge
  \operatorname{RelationInclusion}(S,R,A,B).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Two relations are equal when they contain exactly the same ordered pairs.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:relation-inclusion]{Relation Inclusion}
  \item \hyperref[def:set-equality]{Set Equality}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Union of Relations)}
\begin{definition}[Union of Relations]\label{def:relation-union}
Let $R,S\subseteq A\times B$. The \emph{union} $R\cup S$ is the relation with
\[
(a,b)\in R\cup S
\;\Longleftrightarrow\;
(a,b)\in R \text{ or } (a,b)\in S.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall a\in A\,\forall b\in B,\quad
  (a,b)\in R\cup S
  \iff
  ((a,b)\in R\vee(a,b)\in S).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationFrom}(R\cup S,A,B)
  \iff
  \forall a\in A\,\forall b\in B,\ ((a,b)\in R\cup S \leftrightarrow ((a,b)\in R\vee(a,b)\in S)).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Union of relations keeps every ordered pair that appears in at least one of
the two relations.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:union]{Union}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Intersection of Relations)}
\begin{definition}[Intersection of Relations]\label{def:relation-intersection}
Let $R,S\subseteq A\times B$. The \emph{intersection} $R\cap S$ is the relation
with
\[
(a,b)\in R\cap S
\;\Longleftrightarrow\;
(a,b)\in R \text{ and } (a,b)\in S.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall a\in A\,\forall b\in B,\quad
  (a,b)\in R\cap S
  \iff
  ((a,b)\in R\wedge(a,b)\in S).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationFrom}(R\cap S,A,B)
  \iff
  \forall a\in A\,\forall b\in B,\ ((a,b)\in R\cap S \leftrightarrow ((a,b)\in R\wedge(a,b)\in S)).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Intersection of relations keeps exactly the ordered pairs that appear in both
relations.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:intersection]{Intersection}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Difference of Relations)}
\begin{definition}[Difference of Relations]\label{def:relation-difference}
Let $R,S\subseteq A\times B$. The \emph{difference} $R\setminus S$ is the
relation with
\[
(a,b)\in R\setminus S
\;\Longleftrightarrow\;
(a,b)\in R \text{ and } (a,b)\notin S.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall a\in A\,\forall b\in B,\quad
  (a,b)\in R\setminus S
  \iff
  ((a,b)\in R\wedge(a,b)\notin S).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationFrom}(R\setminus S,A,B)
  \iff
  \forall a\in A\,\forall b\in B,\ ((a,b)\in R\setminus S \leftrightarrow ((a,b)\in R\wedge(a,b)\notin S)).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Difference of relations keeps the ordered pairs in $R$ after removing the
ordered pairs that also lie in $S$.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:set-difference]{Set Difference}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Complement of a Relation)}
\begin{definition}[Complement of a Relation]\label{def:relation-complement}
Let $R\subseteq A\times B$. The \emph{complement} of $R$ relative to
$A\times B$ is
\[
(A\times B)\setminus R.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \forall a\in A\,\forall b\in B,\quad
  (a,b)\in (A\times B)\setminus R
  \iff
  (a,b)\notin R.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationFrom}((A\times B)\setminus R,A,B)
  \iff
  \forall a\in A\,\forall b\in B,\ ((a,b)\in (A\times B)\setminus R \leftrightarrow (a,b)\notin R).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The complement of a relation is computed relative to a fixed Cartesian
product, so it consists of the missing ordered pairs in that ambient product.
\end{remark*}

\begin{remark*}[Exposition]
Relation inclusion, equality, union, intersection, difference, and complement
are inherited from the corresponding set operations on subsets of $A\times B$.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:set-difference]{Set Difference}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem (Converse Laws for Relation Operations)}
\begin{theorem}[Converse Laws for Relation Operations]\label{thm:relation-converse-laws}
Let $R,S\subseteq A\times B$, let $T\subseteq B\times C$, and take complements
relative to the indicated Cartesian products. Then
\[
(R^{-1})^{-1}=R,\qquad
(T\circ R)^{-1}=R^{-1}\circ T^{-1},
\]
\[
(R\cup S)^{-1}=R^{-1}\cup S^{-1},\qquad
(R\cap S)^{-1}=R^{-1}\cap S^{-1},
\]
\[
(R\setminus S)^{-1}=R^{-1}\setminus S^{-1},\qquad
((A\times B)\setminus R)^{-1}=(B\times A)\setminus R^{-1}.
\]
\hyperref[prf:relation-converse-laws]{Go to proof.}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
  \forall R,S\subseteq A\times B\,\forall T\subseteq B\times C,
  \quad
  (R^{-1})^{-1}=R
  \wedge
  (T\circ R)^{-1}=R^{-1}\circ T^{-1},
\]
with converse also preserving union, intersection, difference, and relative
complement as stated above.
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationEquality}((R^{-1})^{-1},R,A,B)
  \wedge
  \operatorname{RelationEquality}((T\circ R)^{-1},R^{-1}\circ T^{-1},C,A).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Taking converses reverses the order of relation composition and commutes with
the Boolean operations inherited from sets.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:converse-relation]{Converse Relation}
  \item \hyperref[def:relation-composition]{Composition of Relations}
  \item \hyperref[def:relation-union]{Union of Relations}
  \item \hyperref[def:relation-intersection]{Intersection of Relations}
  \item \hyperref[def:relation-difference]{Difference of Relations}
  \item \hyperref[def:relation-complement]{Complement of a Relation}
\end{itemize}
\end{dependencies}

\begin{theorembox}{Theorem (Relation Composition and Boolean Operations)}
\begin{theorem}[Relation Composition and Boolean Operations]\label{thm:relation-composition-boolean}
Let $P,Q\subseteq A\times B$, $R\subseteq B\times C$, and
$S,T\subseteq B\times C$. Then
\[
R\circ(P\cup Q)=(R\circ P)\cup(R\circ Q),
\qquad
(S\cup T)\circ P=(S\circ P)\cup(T\circ P).
\]
Also,
\[
R\circ(P\cap Q)\subseteq(R\circ P)\cap(R\circ Q),
\qquad
(S\cap T)\circ P\subseteq(S\circ P)\cap(T\circ P).
\]
The analogous formulas for difference and complement hold only as inclusions
or under extra hypotheses; they are not general distributive laws.
\hyperref[prf:relation-composition-boolean]{Go to proof.}
\end{theorem}
\end{theorembox}

\begin{remark*}[Standard quantified statement]
\[
  \forall P,Q\subseteq A\times B\,\forall R\subseteq B\times C,\quad
  R\circ(P\cup Q)=(R\circ P)\cup(R\circ Q),
\]
and relation composition over intersections gives the stated inclusions in
general.
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationEquality}(R\circ(P\cup Q),(R\circ P)\cup(R\circ Q),A,C)
  \wedge
  \operatorname{RelationInclusion}(R\circ(P\cap Q),(R\circ P)\cap(R\circ Q),A,C).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Composition distributes exactly over union because a middle witness can be
chosen from either side of the union. For intersection, the two composed
relations may use different middle witnesses.
\end{remark*}

\begin{remark*}[Exposition]
Relation composition distributes over unions on both sides. It does not
generally distribute over intersections, differences, or complements. The
reason is existential: a pair can lie in both composed relations using two
different middle witnesses, even when no single witness satisfies both
conditions at once.
\end{remark*}

\begin{remark*}[Exposition]
\mbox{}\par\smallskip
\small
\begin{tabularx}{\textwidth}{l X X}
\toprule
\textbf{Operation} & \textbf{Converse} & \textbf{Composition} \\
\midrule
Union
& $(R\cup S)^{-1}=R^{-1}\cup S^{-1}$
& Distributes exactly on both sides \\
Intersection
& $(R\cap S)^{-1}=R^{-1}\cap S^{-1}$
& Gives $\subseteq$ in general; equality needs extra hypotheses \\
Difference
& $(R\setminus S)^{-1}=R^{-1}\setminus S^{-1}$
& No general distributive equality \\
Complement
& $((A\times B)\setminus R)^{-1}=(B\times A)\setminus R^{-1}$
& No general distributive equality \\
Composition
& $(T\circ R)^{-1}=R^{-1}\circ T^{-1}$
& Associative with identity relation $\Delta_A$ \\
\bottomrule
\end{tabularx}
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation-composition]{Composition of Relations}
  \item \hyperref[def:relation-union]{Union of Relations}
  \item \hyperref[def:relation-intersection]{Intersection of Relations}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Transitive Closure)}
\begin{definition}[Transitive Closure]\label{def:relation-transitive-closure}
Let $R$ be a relation on $A$. The \emph{transitive closure} of $R$ is the
smallest transitive relation on $A$ containing $R$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  T \text{ is the transitive closure of } R
  \iff
  R\subseteq T
  \wedge \operatorname{Transitive}(T,A)
  \wedge
  \forall U,\ ((R\subseteq U\wedge \operatorname{Transitive}(U,A))\to T\subseteq U).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationInclusion}(R,T,A,A)
  \wedge
  \operatorname{Transitive}(T,A)
  \wedge
  \forall U,\ ((\operatorname{RelationInclusion}(R,U,A,A)\wedge \operatorname{Transitive}(U,A))\to \operatorname{RelationInclusion}(T,U,A,A)).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The transitive closure adds exactly the relation pairs needed to make
transitive chaining valid.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:transitive]{Transitive Relation}
  \item \hyperref[def:relation-inclusion]{Relation Inclusion}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Equivalence Closure)}
\begin{definition}[Equivalence Closure]\label{def:relation-equivalence-closure}
Let $R$ be a relation on $A$. The \emph{equivalence closure} of $R$ is the
smallest equivalence relation on $A$ containing $R$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  E \text{ is the equivalence closure of } R
  \iff
  R\subseteq E
  \wedge \operatorname{EquivalenceRelation}(E,A)
  \wedge
  \forall U,\ ((R\subseteq U\wedge \operatorname{EquivalenceRelation}(U,A))\to E\subseteq U).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{RelationInclusion}(R,E,A,A)
  \wedge
  \operatorname{EquivalenceRelation}(E,A)
  \wedge
  \forall U,\ ((\operatorname{RelationInclusion}(R,U,A,A)\wedge \operatorname{EquivalenceRelation}(U,A))\to \operatorname{RelationInclusion}(E,U,A,A)).
\]
\end{remark*}

\begin{remark*}[Interpretation]
The equivalence closure adds exactly the relation pairs needed to make the
relation reflexive, symmetric, and transitive.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:equivalence-rel]{Equivalence Relation}
  \item \hyperref[def:relation-inclusion]{Relation Inclusion}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (One-to-Many Relation)}
\begin{definition}[One-to-Many Relation]\label{def:one-to-many-relation}
Let $R\subseteq A\times B$.
$R$ is \emph{one-to-many} if some input is related to two distinct outputs:
\[
\exists a\in A\,\exists b_1,b_2\in B,\quad
(a,b_1)\in R\land (a,b_2)\in R\land b_1\neq b_2.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \exists a\in A\,\exists b_1,b_2\in B,\quad
  (a,b_1)\in R\wedge(a,b_2)\in R\wedge b_1\neq b_2.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{OneToManyRelation}(R,A,B)
  \iff
  \exists a\in A\,\exists b_1,b_2\in B,\ ((a,b_1)\in R\wedge(a,b_2)\in R\wedge b_1\neq b_2).
\]
\end{remark*}

\begin{remark*}[Interpretation]
One-to-many means that at least one input has two distinct related outputs.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Many-to-One Relation)}
\begin{definition}[Many-to-One Relation]\label{def:many-to-one-relation}
Let $R\subseteq A\times B$.
$R$ is \emph{many-to-one} if two distinct inputs are related to a common output:
\[
\exists a_1,a_2\in A\,\exists b\in B,\quad
(a_1,b)\in R\land (a_2,b)\in R\land a_1\neq a_2.
\]
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \exists a_1,a_2\in A\,\exists b\in B,\quad
  (a_1,b)\in R\wedge(a_2,b)\in R\wedge a_1\neq a_2.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{ManyToOneRelation}(R,A,B)
  \iff
  \exists a_1,a_2\in A\,\exists b\in B,\ ((a_1,b)\in R\wedge(a_2,b)\in R\wedge a_1\neq a_2).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Many-to-one means that two distinct inputs share at least one related output.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Many-to-Many Relation)}
\begin{definition}[Many-to-Many Relation]\label{def:many-to-many-relation}
Let $R\subseteq A\times B$.
$R$ is \emph{many-to-many} if it is one-to-many and many-to-one.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  R \text{ is many-to-many}
  \iff
  \operatorname{OneToManyRelation}(R,A,B)
  \wedge
  \operatorname{ManyToOneRelation}(R,A,B).
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{ManyToManyRelation}(R,A,B)
  \iff
  \operatorname{OneToManyRelation}(R,A,B)
  \wedge
  \operatorname{ManyToOneRelation}(R,A,B).
\]
\end{remark*}

\begin{remark*}[Interpretation]
Many-to-many combines both failures of uniqueness: one input can branch to
multiple outputs, and multiple inputs can share an output.
\end{remark*}

\begin{remark*}[Exposition]
The one-to-many, many-to-one, and many-to-many labels classify how many inputs
and outputs can be connected by a relation.
\end{remark*}

\begin{remark*}[Exposition]
A one-to-many relation fails the determinacy condition needed for a function:
one input admits at least two possible outputs. A many-to-one relation may
still be a function, because different inputs are allowed to share an output.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:one-to-many-relation]{One-to-Many Relation}
  \item \hyperref[def:many-to-one-relation]{Many-to-One Relation}
\end{itemize}
\end{dependencies}

\begin{definitionbox}{Definition (Many-Place Relation)}
\begin{definition}[Many-Place Relation]\label{def:many-place-relation}
A \emph{ternary relation} among sets $A$, $B$, and $C$ is a subset of
$A\times B\times C$. More generally, an $n$-place relation on
$A_1,\ldots,A_n$ is a subset of
\[
A_1\times\cdots\times A_n.
\]
For example, betweenness in geometry is naturally a three-place relation, and
the arithmetic statement $x+y=z$ is naturally a three-place relation among
$x$, $y$, and $z$.
\end{definition}
\end{definitionbox}

\begin{remark*}[Standard quantified statement]
\[
  \operatorname{ManyPlaceRelation}(R,n,(A_i)_{i=1}^n)
  \iff
  R\subseteq A_1\times\cdots\times A_n.
\]
\end{remark*}

\begin{remark*}[Predicate reading]
\[
  \operatorname{ManyPlaceRelation}(R,n,(A_i)_{i=1}^n)
  \iff
  R\subseteq A_1\times\cdots\times A_n.
\]
\end{remark*}

\begin{remark*}[Interpretation]
An $n$-place relation relates $n$ coordinates at a time rather than just two.
\end{remark*}

\begin{remark*}[Exposition]
The hierarchy used in the next chapter is:
\[
\begin{aligned}
\text{relation}
&\longrightarrow
\text{functional relation}
\longrightarrow
\text{function} \\
&\longrightarrow
\text{one-one or onto function}
\longrightarrow
\text{bijection}.
\end{aligned}
\]
The first step adds determinacy; the second adds totality; the later steps
control whether outputs are shared or missed.
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item \hyperref[def:relation]{Relation}
  \item \hyperref[def:cartesian-product-rel]{Cartesian Product}
\end{itemize}
\end{dependencies}
