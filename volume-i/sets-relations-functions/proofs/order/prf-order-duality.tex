\newpage
\phantomsection
\label{prf:order-duality}
\LRAProofFor{prop:order-duality}

\begin{remark*}[Return]
\hyperref[prop:order-duality]{Return to Theorem (\texorpdfstring{\hyperref[prf:order-duality).}
\end{remark*}

\begin{theorem*}[\texorpdfstring{\hyperref[prf:order-duality]
{Duality Principle for Posets}}{Duality Principle for Posets}]
Let $\Phi$ be any first-order statement about a poset $(A, \leq)$ expressed
using only the relation $\leq$.  Let $\Phi^*$ be the statement obtained from
$\Phi$ by replacing every occurrence of $\leq$ with $\geq$ (equivalently,
working in the dual poset).  Then:
\[
  (A, \leq) \models \Phi
  \;\;\Longleftrightarrow\;\;
  (A, \geq) \models \Phi.
\]
In particular, if $\Phi$ is a theorem about all posets, then so is $\Phi^*$.
\end{theorem*}

\begin{proof}
\textbf{Professional Standard Proof.}~
TODO: professional standard proof for order-duality.
\end{proof}

\begin{proof}
\textbf{Detailed Learning Proof.}~
TODO: detailed learning proof for order-duality.
\end{proof}

\begin{remark*}[Proof structure]
\end{remark*}

\begin{dependencies}
\begin{itemize}
  \item TODO: list mathematical dependencies.
\end{itemize}
\end{dependencies}

\clearpage
