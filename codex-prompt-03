# codex-prompt-03: Normal Forms negation normal form

You are working in `wsollers/lra-volume-i`.

Process this prompt only. Do not process later `codex-prompt-*` files until instructed. Execute exactly, then delete `codex-prompt-03`, commit, and report.

## Required rule loading

Before editing, read and obey `AGENTS.md`, `docs/agent-task-index.md`, the available lra-volume overlay, `docs/governance/atomic-artifact-standards.md`, `docs/governance/dependency-standards.md`, `docs/governance/proof-standards.md`, `docs/governance/notation-standards.md`, `docs/governance/extraction-standards.md`, and the relevant schema files under `constitution/schema/`.

Do not edit `common/`, generated governance wrappers, bibliography files, or canonical YAML files.

## Scope

Populate exactly this notes file:

```text
volume-i/propositional-logic/notes/normal-forms/negation-normal-form.tex
```

Create exactly this proof stub:

```text
volume-i/propositional-logic/proofs/normal-forms/prf-existence-negation-normal-form-propositional-logic.tex
```

If `volume-i/propositional-logic/proofs/index.tex` does not yet include `\input{volume-i/propositional-logic/proofs/normal-forms/index}`, add that input after the algebra proof input. Add the proof stub input to `volume-i/propositional-logic/proofs/normal-forms/index.tex` if absent.

## Existing dependency labels to use

- `def:literal-propositional-logic` -> Literal
- `def:well-formed-formula` -> Well-Formed Formula
- `def:logical-equivalence-propositional-logic` -> Logical Equivalence
- `thm:conditional-equivalence-laws-propositional-logic` -> Conditional Equivalence Laws
- `thm:biconditional-equivalence-laws-propositional-logic` -> Biconditional Equivalence Laws
- `thm:de-morgan-laws-propositional-logic` -> De Morgan Laws for Propositional Logic
- `thm:basic-boolean-equivalence-laws-propositional-logic` -> Basic Boolean Equivalence Laws
- `thm:replacement-of-logical-equivalents-propositional-logic` -> Replacement of Logical Equivalents
- `thm:structural-induction-propositional-formulas` -> Structural Induction for Propositional Formulas

## Required artifacts, in order

Generate these artifacts in this exact order:

1. `def:negation-normal-form-propositional-logic` -> Negation Normal Form
2. `def:nnf-conversion-procedure-propositional-logic` -> NNF Conversion Procedure
3. `thm:existence-negation-normal-form-propositional-logic` -> Existence of Negation Normal Form

## Mathematical content

- A formula is in negation normal form if it uses only propositional variables, negated propositional variables, conjunction, and disjunction, and every negation occurs immediately before a propositional variable.
- The NNF conversion procedure:
  1. eliminate biconditionals using biconditional equivalence laws;
  2. eliminate conditionals using conditional equivalence laws;
  3. eliminate double negations;
  4. push negations inward using De Morgan laws.
- The theorem states: for every `\varphi\in\WFF`, there exists `\psi\in\WFF` such that `\psi` is in negation normal form and `\varphi\equiv\psi`.

## Dependency guidance

- NNF depends on `def:literal-propositional-logic`, `def:well-formed-formula`, and `def:logical-equivalence-propositional-logic`.
- NNF Conversion Procedure depends on `thm:conditional-equivalence-laws-propositional-logic`, `thm:biconditional-equivalence-laws-propositional-logic`, `thm:de-morgan-laws-propositional-logic`, and `thm:basic-boolean-equivalence-laws-propositional-logic`.
- Existence of NNF depends on `def:negation-normal-form-propositional-logic`, `def:nnf-conversion-procedure-propositional-logic`, `thm:replacement-of-logical-equivalents-propositional-logic`, and `thm:structural-induction-propositional-formulas`.

## Proof stub requirements

The proof stub must follow `docs/governance/proof-standards.md`. It must start with `\newpage`, include `\phantomsection`, proof label `\label{prf:existence-negation-normal-form-propositional-logic}`, `\LRAProofFor{thm:existence-negation-normal-form-propositional-logic}`, return navigation to the theorem, a starred theorem restatement, TODO professional proof, TODO detailed learning proof, proof-structure remark, dependencies, and `\clearpage`.

The theorem in the notes file must include `\noindent\hyperref[prf:existence-negation-normal-form-propositional-logic]{Go to proof.}`.

## Validation

Run, if available:

```bash
python scripts/validate_leaf_proofs.py --root . --strict
latexmk -lualatex main.tex
```

## Cleanup and commit

Delete `codex-prompt-03` before committing.

Commit message:

```text
Add Volume I negation normal form notes
```

Final report must include files changed, proof stub created, validation status, build status, commit hash, and confirmation that `codex-prompt-03` was deleted.
